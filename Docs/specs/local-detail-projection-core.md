# 局部超特寫：第二階段獨立核心

日期：2026-09-09。對應[正式規格](local-detail-prompt.md)。

## 腰線與紮衣層次第一批（2026-09-09）

前兩批已推送為 `8aaccf3`。本批新增 `localDetailWaistSources.js`，先核對完整上下身配對，再解除舊的未知下身 barrier；外套仍位於這組配對之外。

- 上衣：高領連身上衣、長版寬鬆麻花針織毛衣、上身分類長版襯衫。
- 下身：直筒牛仔褲、運動棉褲、皮革迷你裙、絲質長裙。必須只有一件褲／裙，且原文完全吻合；不是以「牛仔」「低腰」關鍵字套用全目錄。
- 高腰：中央肚臍位置由下身衣料覆蓋，保留下身自己的配色；低腰／超低腰只解除下身的中央遮擋，仍由內層上衣決定可見表面，不表示皮膚露出。正常腰線／缺少腰線時肚臍關係未知；腹側也不從中央推廣成露出。
- 高領連身上衣視為下身內層。兩種長上衣正常／自然放出時在腰頭之外；明確紮入時把腰頭置於外層。長襯衫開合本身仍未知，不因紮衣就改成扣好或露膚。
- 半紮、下擺打結、扣子解開拉鏈微開、非標準版型／圖案、吊帶褲、其他上下身組合、完整配色方案及頸腰飾仍待審核。已有未知外套／配件不可因本規則跳過。

### 特寫提早返回的來源缺口

實際生成測試發現 `buildWardrobe` 的特寫路徑在建立 synthetic modifiers 前返回：單品仍在，但已鎖定腰線可能不在 resolved slots。局部 bridge 現在只補回「明確鎖定＋對應單品存在＋沒有連身／套裝／特殊造型接管」的上／下身版型、上身穿法、下身腰線及圖案來源。缺少或隨機值不重新抽選；已有來源不覆蓋。原始 wardrobe、selection、六輸出與 random 序列不變，補回內容只進 local snapshot。這是針對已選來源的保留，不是假裝原流程已解析所有 modifier。

測試先失敗再實作，驗證外放／紮入順序、高低腰內搭遮蔽、下身配色、未知組合與來源失效；實際 engine fixture 驗證五官特寫下的高腰、半紮／圖案保護及未指定腰線不 reroll。

最終：局部 102/102、frontend 929/929、Prompt Quality 168/168、lint/build、diff check 通過。Strict audit 200、`prompt-quality-baseline` 前後 stdout 一致，零阻擋、23 既有診斷；原六輸出 hash 與 random gate 不變。Browser 於既定 localhost、1440×1000／390×900 完成生成與五工作區導覽／截圖，無新 console warn/error、破圖或 document 溢出。既有手機動作／場景容器溢出保留，測試分頁與 server 已關閉；未修改 Saved Cards 或進行外部生圖。局部消費者仍未接入，不宣稱第七卡驗收。

## 軀幹覆蓋第二批：外套局部表面（2026-09-09）

本批接續未提交的第一批，新增 `localDetailOuterSources.js` 與來源測試，不修改目錄、主 renderer、UI 或 codec。

- 新增來源：西裝外套、丹寧外套、連帽外套、連帽外套_戴、柔軟毛絨泰迪熊外套、薄紗輕薄披衣外套、短版粗花呢外套、蕾絲罩衫。只取各自明確的局部材質，不搬入袖口、帽子、完整衣長。
- 正常／敞開與相容的半扣／半拉鏈可保留局部表面；半開位置不足以解除整片胸口遮蔽。非標準版型、圖案、失效原文、不相容開合仍為 barrier。這是局部輸出的相容性保護，不是修復一般輸出先前暫緩的外套開合問題。
- 雙肩滑落只解除外層鎖骨區域，底下高領仍遮住鎖骨。單肩不視為雙側露出；戴帽連帽外套有額外帽體遮擋，暫不沿用此雙肩釋放規則。
- 薄紗保留 `translucent gauze mesh fabric`，實際局部開合未知，不直接展示底下皮膚／內搭。蕾絲罩衫只在正常穿著、來源明確 long open-front 時讓中央肚臍層繼續讀內搭；其他腹部區域仍未知，無內搭來源不視為裸露。
- 短版外套不從名稱推定肚臍露出。獨立褲裙腰線／紮衣、剩餘外套、其他頸腰飾與特殊造型仍待後續批次；目前不是完整第七輸出。

驗證：新增失敗測試後實作；局部 93/93、frontend 920/920、Prompt Quality 168/168、lint/build 通過。Strict audit 200、`prompt-quality-baseline` 前後 stdout 一致，零阻擋、23 既有診斷。原六輸出 hash／random gate 未變。

Browser：既定 localhost URL，1440×1000、390×900，文字生成與五工作區導覽／截圖檢視，無新 console warn/error、破圖、document 橫向溢出。手機動作／場景既有容器溢出保留。未操作使用者卡片或付費生圖；分頁關閉、server 停止。局部新資料以真實目錄的 resolved adapter 測試驗證，不把尚未接入 UI 的 bundle 宣稱為第七卡 Browser 驗收。

## 軀幹覆蓋第一批（2026-09-09，最新增量）

眼部第二批已推送 `a847cba`。以下新增於 `localDetailTorsoSources.js`，接在同次 resolved adapter；不改知識庫、原六輸出、UI 或 codec。後文六款來源與舊階段數字保留作歷史，以下取代其相應限制。

| 已審核來源 | 胸口／鎖骨 | 腹部／肚臍 |
| --- | --- | --- |
| 高領針織上衣、高領連身上衣 | 頸根、鎖骨、上胸覆蓋，僅保留針織／彈性布料 | 前者仍未新增腹部規則；後者保留既有連續覆蓋 |
| 襯衫、絲綢緞面襯衫、短袖上衣、落肩 T 恤 | 保留府綢／緞面／彈性布料／棉質 jersey；局部開口未知，不臆造露膚 | 未新增衣襬／腹部規則 |
| 棉質細肩背心、絲質細肩帶上衣、短版吊帶背心 | 保留局部肩帶與該上衣配色，不搬入完整上身，不承諾整片胸口露出 | 短版名稱不代表肚臍一定露出 |
| 長版寬鬆麻花針織毛衣 | 上胸布料覆蓋 | 長版來源覆蓋腹部三區，只輸出局部針織質地，不輸出大腿、袖長或隱藏肚臍環 |
| 上身分類長版襯衫 | 局部府綢，開合未知 | 同左；長度來源不等於扣子已扣好，也不推定敞開 |
| 外套分類長版襯衫 | 正常／單肩／雙肩、正常或半扣或敞開來源需精確匹配；保留局部府綢。雙肩僅解除外層鎖骨遮蔽，再讀內搭；單肩不將雙側鎖骨標成露出，上胸仍未知 | 保留正常穿著＋敞開的中央開口規則；其餘位置與滑肩狀態仍未知 |

未知／失效來源不能 fallback 到較鬆的同名 substring 判定；尤其外套「正常穿著」不再只用中文名稱判斷。只有明確 exposed 的最內層才允許既有露膚／肚臍環片語。覆蓋未知但局部表面已確認時，仍可輸出布料，不代表已完成該區全部可見內容。

本批仍不處理：獨立褲裙腰線、其餘外套、非標準版型／圖案／衣襬改穿法、其他頸腰飾、胸口髮絲遮蔽、角色卡／特殊造型／複合配色。這些保護性缺資料狀態不是全款式驗收；不可直接跳過來源擴充就宣稱第七輸出完成。胸口膚質也未藉本批強制補入。

測試先建立失敗案例再實作，涵蓋局部衣料、兩層遮蔽、單／雙肩、短版不推定露腹、未知褲裙、來源失效、衣襬 modifier 及配色歸屬。Runtime 使用實際解析結果驗證白色長毛衣與白外層／黑內搭；原六輸出 hash 與 random 基準仍是獨立回歸關卡。

最終驗證：局部 81/81、frontend 908/908、Prompt Quality 168/168、lint/build、`git diff --check` 通過。Strict audit（200、`prompt-quality-baseline`）前後 stdout 完全相同，零阻擋、23 個既有診斷；build 僅既有 chunk-size 提示。新增配色測試曾因 `/red/` 誤中 `centered` 而失敗，改用單字邊界後重跑局部／全套均通過，未為此改動產品文字。

Browser：既定 localhost URL，1440×1000、390×900，生成＋五工作區導覽；最終程式修改後兩尺寸再生成，六輸出標題與 DLL 來源仍保持原狀。無 console warn/error、破圖或 document 橫向溢出；手機動作／場景面板仍有先前記錄的容器溢出，不在本次 UI/CSS 範圍。畫面由工具截圖檢視，未新增截圖檔、未更動 Saved Cards、未進行外部生圖。測試分頁關閉且 dev server 已停止。這是既有 UI 回歸，不是尚未接入的第七卡功能驗收。

## 已實作與尚未接入

`localDetailProjection.js` 已能將經審核、帶來源引用的局部模型轉成三部位英文結果。`localDetailSourceAdapter.js` 提供 6 款原始目錄衣物的區域轉換。後續同日已由 `localDetailResolvedAdapter.js` 接入 `engine.js` 的同次解析資料（詳見下方 runtime 小節），但尚未接入 UI／storage consumer；不代表整個目錄或第七輸出完成。

目前可審核來源：

| 款式 | 目標與證據範圍 |
| --- | --- |
| 亮面乳膠束帶套裝 | 腹部連續不透明覆蓋，保留局部乳膠，不露出被遮住的肚臍環 |
| 短版高領挖腰連身泳裝 | 肚臍位置明確挖空，不搬入高領胸片、完整下身 |
| 短版亮面乳膠拉鏈洋裝 | 肚臍位置為中央狹長開口；腹側不自動標成露出 |
| 一字領上衣 | 鎖骨開口，不把整片上胸判成露膚 |
| 透膚刺繡襯衫 | 上胸保留已知半透明衣料，底下皮膚／內搭仍待資料 |
| 高領連身上衣 | 連續軀幹覆蓋，保留來源的布料選擇語句，不自行選定其中一種 |

原始來源轉換器不包含改穿法後的狀態；有 effective modifier 時保守回傳 unknown 層。Runtime bridge 僅額外辨識來源明確的標準上衣版型／正常穿著，以及正常穿著、敞開的長版襯衫中央開口；其他 modifier 仍待專門規則。未知款式、特殊穿搭、角色卡及來源失效不能沿用其他款式。

## 核心輸入契約

`buildLocalDetailBundle(snapshot)` 的輸入是內部模型，不是儲存 schema：

- `subjectCount`：僅數字 1 支援，其餘回傳空 bundle。
- `imageType`：既有 6 個成品類型 ID；未知類型不降級成攝影。
- `sources`：同一次解析的來源字典，key 對應來源物件的 `en`。
- `targets[target].layers`：**實際所有可能遮擋該區域的層次**，由外到內。層包含 `id`、`regions`、`details`。不能拿主景別裁切後的服裝填入，也不能漏掉未知外層。
- `regions[region]`：`state` 與 `ref: { key, excerpt }`。狀態為 exposed／covered／translucent／unknown；缺少區域或證據失效一律 unknown。exposed 只解除這一層，仍繼續讀內層。
- 層上的 `details`：經審核、確實位於該區域的衣料／衣物結構來源，帶 `group`、`region`、`ref`。
- 目標上的 `details`：被上述層次遮擋的皮膚、身份或配件來源；只有確定 exposed 的子區域才會輸出。無法直接透過 translucent 自動展示底下飾品。
- `effects`：已由 adapter 審核為局部適用的 lighting／imaging 來源片語。不是整段場景或攝影師文字的自動拆句器；目前無完整目錄效果 adapter。

核心驗證 excerpt 確實存在於來源，但**文字存在不等於語意已審核**。呼叫端必須提供已審核的部位與層次對應，不能把任何來源句都標成 localFabric。來源轉換器的有效 modifier、角色卡與完整造型優先順序仍須接到當次原始 resolved snapshot，不能由本核心猜測。

回傳三份不可變結果，包含固定 id／label／target／contractVersion、status、text、coverage、coverageState、sourceRefs、diagnostics。coverageState 的 mixed 有逐子區域內容；diagnostics 不進公開英文。這些欄位尚不是 Saved Cards 保存契約。

## 已驗證行為

- 已知不透明層遮蔽底下飾品；外層敞開／半透明不能移除內搭。
- 未知外層阻擋內層；沒有任何層不等於裸露。
- 有可信局部表面時可以保留衣料而不推定皮膚；完全缺資料則英文為空、status 為 needs-source-review。
- 鎖骨露出或肚臍中央開口不推廣至其他子區域。
- 錯誤 group、錯誤 region、失效 excerpt 不成為公開內容。
- 三目標使用同一輸入、無隨機呼叫、不修改來源；額外加入場景、鞋子、姿勢、一般景別不影響結果。
- 水彩、油畫、時裝插畫、粉彩不被改成寫實攝影。核心不繼承一般鏡頭，也不新增背景或水中濕潤感。

局部測試 33/33（原契約 13、投影 13、真實目錄 adapter 7）；完整 frontend 860/860；Prompt Quality 168/168；lint／build 通過，僅既有 chunk-size advisory。Strict audit 使用 200、seed `prompt-quality-baseline`，前後 stdout 完全一致，零阻擋、23 既有診斷。

## 下一個接入關卡

1. 擴充 resolved context 的可審核層次，補齊圖案、有效 modifier、配件遮蔽、角色卡及各種完整造型；目前已串接解析結果，不等於完整款式支援。
2. 同次解析／零新增 random／舊六逐字不變的 runtime gate 已通過；消費者實際切換部位的 UI gate 尚待接入。
3. 接入第七卡、複製、生成來源與 Saved Cards 新舊 round-trip。
4. 第七卡接入後執行其 desktop／mobile Browser QA 與交付實測英文；目前僅完成既有工作區回歸。

## Runtime bridge 與驗證（2026-09-09）

`engine.js` 在同一次生成、原始角色／服裝／配色已解析後呼叫 `buildResolvedLocalDetailBundle`，三目標結果放在 `localDetailPrompts`，不加入 `extraPrompts`。沒有把景別、俯仰、環繞、焦段、場景、仰躺表面、姿勢或自拍文字傳進局部 adapter。來源字典取自同一組 slot/color 物件，並保留可追溯 excerpt；結果不修改原始物件。

已接入的範圍與限制：

- 上表 6 款衣物，辨識 runtime 已加前綴的連身／套裝名稱。局部布料可以附上同次解析的簡單配色，不搬入整套色系指令；完整造型 palette 仍為未審核 barrier。
- 長版襯衫＋敞開穿＋正常穿著，只將外層的肚臍中央開口視為可通過，仍讀內搭；只有外套且沒有主衣物時，不視為裸露。
- 一般人物眼部支援來源有明確眼部片語的 4 款臉型；已解析髮型、頭部配件與眼前眼鏡遮擋尚未建立完整區域資料時，先阻擋眼部結果。明確戴在頭頂的眼鏡不移到眼前。
- 3 組局部適用膚質、3 組主體光線選項的來源片語可讀取；其餘沒有機械複製完整描述。部位未確認時，光線本身不能使結果變成 ready。
- 肚臍環只有 navelPosition 明確 exposed 才保留。其他腰飾、頸飾、獨立褲裙腰線尚未審核時形成 barrier；不悄悄省略可能遮住部位的物件。
- 外層的露膚描述帶 `requiresExposed`，等所有層次確認後才輸出，避免「外層有開口＋內層有遮蔽」仍寫出裸露皮膚。
- 角色卡、特殊角色與特殊穿搭包含獨立身份／穿戴物，目前回傳待補資料而不是以普通人物代替。雙人 bundle 為空。

`localDetailRuntime.test.js` 的 40 個舊結果基準在接入前取得，排除 id/date 和新增 bundle，包含六輸出、summary、selection 與 structured；單人／雙人的隨機呼叫次數為 651／677，接入後相同。這不代表生圖相同，而是來源與舊文字結果相同。

Runtime 階段最終測試：局部 45/45，完整 frontend 872/872，Prompt Quality 168/168，lint/build 通過。200 筆 `prompt-quality-baseline` strict audit 前後 stdout 相同，零阻擋、23 既有診斷。Saved Cards 測試確認現有 codec 不保存 `localDetailPrompts`，原六文字保留；這是隔離測試，不是第七輸出 round-trip 成功。

Browser URL：`http://127.0.0.1:5175/Virtual_Photography_Studio/`。1440×1000、390×900 均完成隨機生成與五工作區導覽，六份輸出和 DLL 來源清單維持原樣，console warn/error 為空。手機動作／場景面板有既有容器超出（352px 內容／327px 容器），同尺寸公開 Pages 版也重現；本次未動 UI/CSS。Screenshots 在工具內檢視，沒有新增檔案；未操作付費生圖、保存、刪除或匯入使用者卡片。

## 眼部來源擴充第一批（2026-09-09）

前述核心與 bridge 已推送為 `84a4e8e`。後續新增 `localDetailEyeSources.js`，不修改目錄、原六輸出、UI 或 storage。

| 來源 | 目前已審核規則 |
| --- | --- |
| 帥氣濕亮油頭 | 原文完整匹配且包含 `hair combed away from the forehead`；整理狀態為無、柔順自然或濕髮分束時，不阻擋眼部。不把整套短髮或濕髮描述搬入眼部畫面 |
| 黑色口罩、3M 6200 半面呼吸器 | 精確匹配已審核原文時，屬鼻口區域，不阻擋眼部也不強制入鏡 |
| 頭戴耳機、頸掛耳機、有線耳機 | 精確匹配已審核原文時，不阻擋眼部，不為完整展示耳機而拉遠 |
| 眼布 | 保留 `black elastic stretch-fabric eye covering fitted directly over both eyes`，眼睛 covered；不輸出底下眼型、膚質、表情，不繼承其整張臉／髮型說明。眉毛與周圍覆蓋仍 unknown |
| 已戴在頭頂的普通眼鏡 | 保留原本不移回眼前的規則，配戴來源改為完整匹配。眼布不套用此普通眼鏡例外 |
| 13 組眉眼表情來源中的眼部片語 | 只取已選 `en` 內的眼睛／眼瞼片語；`無額外表情` 沒有文字，`撒嬌生氣` 只有眉毛片語，尚不投影至 eyes。不得補嘴型、牙齒、臉頰或視線方向 |

每一層的審核證據保留在內部 `sourceRefs`；非眼部物件的證據不等於公開文字。髮型／配件原文修改後需重新審核，不能只靠相同中文名稱跳過遮蔽。一般眼鏡、墨鏡、其餘髮型與帽飾、微風／強風、眉型投影仍未完成；已支持的 4 組原文眼型也沒有擴張成全部 6 組臉型。

眼部構圖句現在指定 `eye area` 與該位置的可見表面，可呈現眼布，不要求穿透衣料看見眼睛。胸口與腹部投影不變。

本批先加入回歸測試確認舊實作失敗，再實作：局部 54/54、完整 881/881、Prompt Quality 168/168、lint/build 通過；200 筆同種子 strict audit 前後一致（零阻擋、23 既有診斷）。實際 `generatePrompts` 測試覆蓋髮型＋口罩＋表情、眼布抑制身份／表情，並保留舊六結果 SHA 與隨機呼叫次數。桌面／手機完成既有生成與五工作區 smoke；沒有第七卡消費者或外部生圖驗收。

## 眼部來源擴充第二批（2026-09-09）

第一批已推送為 `de12171`。第二批的精確來源清單放在 `localDetailEyeCatalog.js`，不修改原始目錄或六個既有 renderer。以下取代第一批的小範圍限制，但不是全情境支援：

| 區域／來源 | 已實作 | 仍保守處理 |
| --- | --- | --- |
| 六種五官 | 從同一選項的 `mj.face` 結構分別抽出眼型／眉型；不帶臉型、鼻唇、完整身份。source key 為 `character.facialFeatures.mj.face` | 無此欄位的舊 raw snapshot 仍用原 4 組英文眼部錨點，不補眉型；已存在但失效的結構不偷偷 fallback |
| 31 種髮型 | 24 組受控基線不輸出完整頭髮；7 組瀏海／近眼髮絲只保留局部片語、可追溯整理狀態與髮色 | 只適用無整理狀態、柔順自然、濕髮分束。微風／強風與變更後的原文仍阻擋；不從名稱推測新資料 |
| 7 組局部頭髮 | 齊瀏海圓弧鮑伯、不對稱濕感短鮑伯、直髮：日式瀏海、自然微彎：瀏海、柔波：瀏海、輕透齊瀏海內彎鮑伯、輕透瀏海自然微彎長髮 | 輕透／near-eye 不等於能看見完整雙眼或眉毛；眼型、眉型、底下膚質的曝光狀態保留 unknown。這是保留已知局部表面，不是判定整張臉被完全遮住 |
| 8 款一般眼鏡＋太陽眼鏡 | 保留款式與 12 組已選框色；頭頂配戴不強制入鏡；近眼髮絲可以獨立保留 | 鏡框來源不保證透明鏡片，故眼睛／眉毛區域 unknown，不強加其身份／表情。透明框不能改變鏡片透明度；不補眼色 |
| 眼布 | 同次 eyewearColor 的色彩套用在布料，不再固定黑色，不寫成「白色鏡框」 | 未審核的布料配色不沿用黑色 fallback；不因色名就強制底下眼睛可見；不套用頭頂眼鏡規則 |
| 14 組非空表情 | 13 組有眼瞼／眼睛片語，13 組有眉毛片語，按各自子區域投影；自然喜悅只供眼部、撒嬌生氣只供眉部 | 不輸出嘴唇、牙齒、臉頰、全臉情緒。被遮住的區域不能為呈現表情而顯露 |

眉型／眼型是既有結構來源的重用，不是新增控制項，也沒有變更 Gpt／Z-Image／AI 的身份來源。原始 `sources` 字典保留不變，另以明確欄位路徑 key 登錄這個結構字串，讓每個公開片語均可回溯到已解析的選項。

核心新增可見表面類型 `localHairOcclusion`／`eyewearAtEyes` 的既有 group 處理：這些表面可在底下身份未知時輸出，但不解除遮擋；眼布配色只在該表面可見時輸出。眼鏡與附近頭髮的子區域分開處理，不把局部髮絲描述誤當成完整眼睛可見的證據。胸口與腹部目標的輸出不變。

測試先加入失敗案例再實作。最終局部 68/68、完整 frontend 895/895、Prompt Quality 168/168、lint/build 通過；strict audit 200 筆 `prompt-quality-baseline` 前後 stdout 一致，零阻擋、23 既有診斷。測試涵蓋 31×10＝310 個髮型／眼鏡組合、六組五官、全部框色、眉眼遮蔽、來源失效、舊 raw 來源、同次資料不可變及兩個軀幹目標不變。Runtime fixture 驗證冷感臉／微怒眉毛、濕髮＋銀框太陽眼鏡、白色眼布。六個舊結果的 hash 與隨機呼叫次數保持不變。

Browser 在既定 localhost URL、1440×1000／390×900 檢查原六輸出生成及五工作區 smoke；沒有新增 console warning/error、破圖或 document 溢出。既有手機動作／場景容器溢出未更動。沒有 UI／storage consumer，因此這不是第七卡切換、複製、保存的瀏覽器驗收；也未呼叫外部付費生圖。
